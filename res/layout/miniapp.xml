<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#22000000"
    android:onClick="close" >

	<FrameLayout
	    android:id="@+id/webview"
	    android:layout_width="fill_parent"
	    android:layout_height="fill_parent" >

		<WebView
		    android:id="@+id/webView1"
		    android:layout_width="330dp"
		    android:layout_height="460dp"
		    android:layout_gravity="center"
		    android:layout_marginTop="40dp" />

		<ImageView
		    android:id="@+id/animicon"
		    android:layout_width="wrap_content"
		    android:layout_height="wrap_content"
		    android:layout_gravity="center"
		    android:layout_marginTop="-200dp"
		    android:src="@drawable/ic_launcher2" />

	</FrameLayout>

	<RelativeLayout android:id="@+id/reloadimgview"
		android:layout_width="wrap_content" android:layout_height="2000px"
		android:background="#00000000">

		<ImageView android:id="@+id/reloadimg" android:layout_width="wrap_content"
			android:layout_height="wrap_content" android:layout_alignParentLeft="true"
			android:layout_alignParentRight="true" android:layout_alignParentTop="true"
			android:src="@drawable/reloadimg" android:visibility="invisible" />

	</RelativeLayout>
</FrameLayout>